export interface Destination {
  name: string;
  country: string;
  description: string;
  latitude?: number;
  longitude?: number;
}

export interface MapLocation {
  name: string;
  country: string;
  description: string;
  latitude: number;
  longitude: number;
}